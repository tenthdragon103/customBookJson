package com.tenth.guidebook;

import org.bukkit.event.Listener;

public class GuideBookListener implements Listener {

    private final GuideBook plugin;

    public GuideBookListener(GuideBook plugin) {
        this.plugin = plugin;
    }

    //over ride stuff? idk if this class is actually required or not

}
